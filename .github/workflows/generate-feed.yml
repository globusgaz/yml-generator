name: 🛒 Generate Prom Feed

on:
  schedule:
    - cron: '0 * * * *'  # Щогодини (UTC)
  workflow_dispatch:     # Можна запускати вручну

permissions:
  contents: write  # ⬅️ Дозволяє пушити зміни в репозиторій

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Клоную репозиторій
      uses: actions/checkout@v3
      with:
        fetch-depth: 0   # потрібно для git pull --rebase

    - name: 🐍 Встановлюю Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: 📦 Встановлюю залежності
      run: pip install lxml aiohttp

    - name: ⚡ Оновлюю локальну гілку
      run: |
        git config user.name "github-actions"
        git config user.email "github-actions@github.com"
        git pull --rebase

    - name: 🚀 Генерую YML
      run: python main.py

    - name: 📝 Комічу YML-файли у репозиторій
      run: |
        git add output_*.yml
        git diff --cached --quiet || git commit -m "🔄 Автоматичне оновлення YML-файлів"
        git push
