name: 🛒 Generate Prom Feed

on:
  schedule:
    - cron: '0 * * * *'  # Щогодини (UTC)
  workflow_dispatch:     # Можна запускати вручну

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Клоную репозиторій
      uses: actions/checkout@v3
      with:
        fetch-depth: 0   # потрібно для pull/merge

    - name: 🐍 Встановлюю Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: 📦 Встановлюю залежності
      run: pip install lxml aiohttp

    - name: 🚀 Генерую YML
      run: python main.py

    - name: 🧹 Видаляю старі output_*.yml
      run: |
        rm -f output_*.yml || true

    - name: 📝 Комічу all.yml у репозиторій
      run: |
        git config user.name "github-actions"
        git config user.email "github-actions@github.com"

        git fetch origin main
        git merge --strategy-option theirs origin/main || true

        # додаємо тільки all.yml і видалення старих
        git add -A
        git diff --cached --quiet || git commit -m "🔄 Оновлено all.yml"

        git push origin HEAD:main
