name: 🛒 Generate Prom Feed

on:
  schedule:
    - cron: '0 * * * *'  # Щогодини (UTC)
  workflow_dispatch:     # Можна запускати вручну

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Клоную репозиторій
      uses: actions/checkout@v3
      with:
        fetch-depth: 0   # потрібно для reset

    - name: 🐍 Встановлюю Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: 📦 Встановлюю залежності
      run: pip install lxml aiohttp

    - name: 🚀 Генерую YML
      run: python main.py

    - name: 🧹 Видаляю старі YML перед комітом
      run: |
        rm -f prev_feed.yml || true
        rm -f output_*.yml || true

    - name: 📝 Комічу згенеровані файли у репозиторій
      run: |
        git config user.name "github-actions"
        git config user.email "github-actions@github.com"

        # синхронізуємо з віддаленим main, щоб уникнути конфліктів
        git fetch origin main
        git reset --hard origin/main

        # додаємо всі нові/оновлені YML
        git add -A
        git diff --cached --quiet || git commit -m "🔄 Оновлено all.yml"

        git push origin HEAD:main
